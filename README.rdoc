== README

This is a small application done for url shortening purposes.

To run this application You will need:

* PostgreSQL v9.4.0

To set up database configuration take a look at config/database.yml.example

To run tests, run command == rspec from root directory of project in terminal
